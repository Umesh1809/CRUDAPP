apiVersion: v1
kind: Service
metadata:
  name: umeshcrudapp-service
  labels:
    app: umeshcrudapp
spec:
  type: LoadBalancer
  selector:
    app: umeshcrudapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 32778
